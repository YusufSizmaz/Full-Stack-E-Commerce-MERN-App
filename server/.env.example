# Server Configuration
NODE_ENV=development
PORT=8080

# Database
MONGODB_URI=mongodb://localhost:27017/ecommerce

# JWT Secrets (Use strong random strings in production - minimum 32 characters)
SECRET_KEY_ACCESS_TOKEN=your-super-secret-access-token-key-min-32-chars
SECRET_KEY_REFRESH_TOKEN=your-super-secret-refresh-token-key-min-32-chars
JWT_ACCESS_EXPIRY=5h
JWT_REFRESH_EXPIRY=7d

# Frontend URL (for CORS - can be comma-separated for multiple origins)
FRONTEND_URL=http://localhost:5173

# Email Service (Resend)
RESEND_API=your-resend-api-key

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET_KEY=your-api-secret

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Security Notes:
# 1. NEVER commit the actual .env file to git
# 2. Use strong, randomly generated secrets for JWT tokens
# 3. Change all default values in production
# 4. Keep this file updated when adding new environment variables
